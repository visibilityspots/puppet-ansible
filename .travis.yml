sudo: false

before_install:
  - ln -s . ansible

cache:
  - apt
  - bundler

addons:
  apt:
    packages:
    - rpm

rvm:
  - 1.9.3

script:
  - curl -s https://raw.githubusercontent.com/visibilityspots/ci-scripts/master/travis/puppet-module.sh | bash -s ansible development

notifications:
  email:
    - travis@visibilityspots.com
